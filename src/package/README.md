# Packages

The package is consist of follows

- domain
- store
- use-case

In other words, a packages is consist of non-view layer.

## Package

A package

- `<package>`
    - `<name>`
    - `<name>Store`
    - `<name>Repository`
    - `use-case/`
        - `DoSomethingUseCase`
        
